namespace XEntity
{
    //This enum holds the types of items.
    //Add custom item types here.
    public enum ItemType
    {
        Default,
        ToolOrWeapon,
        Consumeable,
        Placeable,
        Armor,
        MagicTool
    }
}
